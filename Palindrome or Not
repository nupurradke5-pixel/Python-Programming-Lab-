# Read the input string
text = input()

# Remove case sensitivity by converting to lowercase (optional, but standard)
# Check if the string is equal to its reverse
if text == text[::-1]:
    print("Palindrome")
else:
    print("Not a Palindrome")
